# 포트폴리오 사이트 관리 가이드

이 사이트는 이제 **`data/works.json`** 파일 하나로 모든 작업(다큐멘터리, 책, 프로젝트 등) 내용을 관리해요.
비밀번호나 "편집 모드" 버튼은 더 이상 없어요 — GitHub에 로그인해서 파일을 직접 고치는 방식이에요.

---

## 1. 작업 하나 수정하기 (예: 설명 글, 링크 바꾸기)

1. GitHub 저장소(`yuristal-svg/yuristal1`)로 들어가요.
2. `data` 폴더 → `works.json` 파일을 클릭해요.
3. 오른쪽 위 연필 아이콘(✏️ Edit this file)을 눌러요.
4. 고치고 싶은 항목을 찾아서 값만 바꿔요. 예를 들어 설명을 바꾸고 싶으면:

```json
"desc": "여기에 새 설명을 적어주세요"
```

5. 다 고쳤으면 아래로 스크롤해서 초록색 **"Commit changes"** 버튼을 눌러요.
6. 1~2분 기다리면 실제 사이트(`https://yuristal-svg.github.io/yuristal1/`)에 반영돼요.

> ⚠️ 주의: 큰따옴표(`"`)와 쉼표(`,`)를 지우거나 잘못 건드리면 사이트 전체가 안 뜰 수 있어요.
> 문장 안의 글자만 바꾸고, 앞뒤에 있는 `"`, `,`, `{`, `}`는 그대로 두세요.

---

## 2. 새 작업(프로젝트) 하나 추가하기

`works.json`을 열면 이렇게 생긴 덩어리가 6개 있어요 (아빠의 기억, 책, WOMEN AND THEIR CHAIRS 등):

```json
{
  "id": "spy-writing",
  "title_en": "Spy Writing (in NAKJAK)",
  "title_kr": "스파이 글쓰기 (나크작 내)",
  "kind": "Writing Project · Facilitator",
  "date": "2025",
  "tags": ["WRITING PROJECT", "FACILITATOR"],
  "desc": "",
  "image": "",
  "link": "",
  "link_label": "",
  "link2": "",
  "link2_label": ""
}
```

새 작업을 추가하려면:

1. 마지막 `}` 다음에 쉼표(`,`)를 넣고, 위와 같은 형태로 새 덩어리를 하나 붙여넣어요.
2. 각 항목의 뜻:
   - `id` : 이 작업의 고유 이름 (영어 소문자, 띄어쓰기 없이 — 예: `"new-project"`)
   - `title_en` / `title_kr` : 영문 제목 / 한글 제목
   - `kind` : 분류 (예: "Documentary Film", "Book")
   - `date` : 연도
   - `tags` : 카드에 표시될 태그 (배열, 여러 개 가능)
   - `desc` : 클릭했을 때 뜨는 설명 (긴 글도 가능)
   - `image` : 사진 경로 (아래 3번 참고, 없으면 `""`로 비워두면 자동으로 텍스트 카드가 떠요)
   - `link`, `link2` : 구매처/참고 링크 (없으면 `""`)
   - `link_label`, `link2_label` : 링크 버튼에 표시될 문구

3. 사진이 없는 항목은 자동으로 검정/노랑 텍스트 카드로 표시돼요. 이 디자인을 바꾸고 싶으면 `index.html` 안에서 `PLACEHOLDER` 라고 검색하면 나오는 부분을 고치면 돼요 (개발자에게 요청하는 게 편해요).

---

## 3. 사진 새로 추가하거나 바꾸기

1. 컴퓨터에 있는 사진 파일 이름을 영어로 바꿔주세요 (예: `father-memory.jpg`).
2. GitHub 저장소 → `assets/images/works` 폴더로 들어가요.
3. **"Add file" → "Upload files"**를 눌러서 사진을 올려요.
4. `works.json`에서 해당 작업의 `"image"` 값을 이렇게 채워요:

```json
"image": "assets/images/works/father-memory.jpg"
```

5. Commit changes를 누르면 반영돼요.

---

## 4. 되도록 지켜야 할 것

- 파일 안의 한글, 영어 텍스트는 자유롭게 고쳐도 안전해요.
- `{`, `}`, `[`, `]`, `"`, `,` 같은 기호는 개수와 위치를 그대로 유지해야 해요 (하나라도 빠지면 사이트가 안 떠요).
- 잘 모르겠으면 통째로 복사해서 저(Claude)에게 "이렇게 바꿔줘"라고 보내주시면 대신 고쳐드릴게요.
- 실수했을 때는 GitHub의 **"History"** 메뉴에서 이전 버전으로 되돌릴 수 있어요.

---

## 5. 파일 구조 요약

```
index.html                ← 사이트 화면 (거의 건드릴 일 없음)
data/works.json           ← 작업 내용 (여기만 고치면 됨)
assets/images/works/      ← 작업 사진들
```
